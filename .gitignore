# Lake build output + fetched dependency packages (Lake materializes pinned
# git deps into .lake/packages from the committed lake-manifest.json lockfile).
/.lake/
/build/
*.olean
*.ilean
*.trace

# Editor / OS
.DS_Store
*.swp
